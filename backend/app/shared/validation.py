def validate_positive_number(val: float) -> bool:
    """
    Validates that a numeric value is strictly positive.
    """
    return val > 0.0

def validate_non_negative_number(val: float) -> bool:
    """
    Validates that a numeric value is non-negative.
    """
    return val >= 0.0
